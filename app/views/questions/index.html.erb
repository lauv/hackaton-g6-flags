<h3>Puntaje <%= session[:score] %></h3>


<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h2>¿Cuál es la bandera de <%= @question.content.humanize %>?</h2>
  </div>
  <div class="col-md-6 col-md-offset-3">
    <% @choices.shuffle.each do |choice| %> 
    <%= link_to questions_check_path(choice_id: choice.id, quiz_id: params[:quiz_id]), method: "post" do %>
      <%= image_tag choice.answer, class: "flag-img" %>
    <% end %>
    
    <% end -%>
  </div>
</div>
<%= link_to 'Salir', root_path, class: "btn btn-danger" %>

<br>
